# Media queries gebruiken

Met media queries kan je stijlen toepassen vanaf/bij bepaalde schermgroottes.

```css
@media (max-width: 768px) {
  .menu {
    flex-direction: column;
  }
}
```

Dit betekent: als het scherm 768px of kleiner is, stapelt het menu verticaal.
