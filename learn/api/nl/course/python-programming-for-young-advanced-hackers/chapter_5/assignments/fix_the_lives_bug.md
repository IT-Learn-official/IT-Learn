# Missie: Fix de lives bug

Doel
- Fix de code zodat `lives` correct verandert en je na elke wijziging de juiste waarde print.

Stappen
1. Open `attachments/fix_the_lives_bug.py`.
2. Zoek en fix de bug zodat de output exact dit is:

```
Lives: 3
Lives: 2
Lives: 1
```

3. Voeg na elke wijziging van `lives` een `print(...)` toe.

