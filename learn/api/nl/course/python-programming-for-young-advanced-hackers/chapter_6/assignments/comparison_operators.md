# Missie: Vergelijkingsoperatoren detective

Doel
- Test alle vergelijkingsoperatoren: `==`, `!=`, `<`, `>`, `<=`, `>=`.

Stappen
1. Open `attachments/comparison_operators.py`.
2. Gebruik elke operator om twee getallen te vergelijken en print het resultaat.
3. Zorg voor minstens 6 `print(...)`-regels (één per operator).

