# Missie: Status line generator

Doel
- Maak één statusregel die meerdere variabelen combineert met een f-string.

Stappen
1. Open `attachments/status_line_generator.py`.
2. Start met variabelen zoals `filename`, `size_kb` en `status`.
3. Bouw één regel met een f-string en print die.

Hint
- Zet `f` voor je string: `f\"...{variable}...\"`.

