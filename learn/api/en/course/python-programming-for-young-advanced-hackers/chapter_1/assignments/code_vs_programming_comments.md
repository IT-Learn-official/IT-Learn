# Mark planning vs code

Task
- Add inline markers to attachments/code_vs_programming_comments.py.

Rules
- Put `# PROGRAMMING` at the end of each planning/comment line.
- Put `# CODE` at the end of each executable Python line.
- Do not delete or reorder lines.
- File must still run without syntax errors.

Validator hint
- Validator expects `name = getName(` and `print("Hello, " + name + "!")` present with the correct markers.
