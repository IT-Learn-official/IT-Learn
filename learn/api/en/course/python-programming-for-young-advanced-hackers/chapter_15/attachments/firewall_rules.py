# Firewall Rule Simulator
# Build classes FirewallRule and Firewall according to the assignment.

# TODO: define class FirewallRule
#   - __init__(self, allowed_ip, allowed_port)
#   - allows(self, ip, port) -> True if both match, otherwise False

# TODO: define class Firewall
#   - __init__(self): create self.rules as an empty list
#   - add_rule(self, rule): add a FirewallRule to the list
#   - check(self, ip, port): return "ALLOW" if any rule matches, else "BLOCK"

# TODO: demo code
#   - create a Firewall
#   - add at least two rules
#   - call check on several (ip, port) combinations and print the results


