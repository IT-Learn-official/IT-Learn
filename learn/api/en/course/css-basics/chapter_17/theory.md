# Using Media Queries

Media queries let you apply styles only at certain screen sizes.

```css
@media (max-width: 768px) {
  .menu {
    flex-direction: column;
  }
}
```

This means: when the screen is 768px or smaller, the menu stacks vertically.
