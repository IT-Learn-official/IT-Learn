# Mission: Fix den Lives-Bug

Ziel
- Repariere den Code so, dass `lives` korrekt verändert wird und du nach jeder Änderung den richtigen Wert ausgibst.

Schritte
1. Öffne `attachments/fix_the_lives_bug.py`.
2. Finde und behebe den Bug, sodass die Ausgabe exakt so ist:

```
Lives: 3
Lives: 2
Lives: 1
```

3. Füge nach jeder Änderung von `lives` ein `print(...)` hinzu.

