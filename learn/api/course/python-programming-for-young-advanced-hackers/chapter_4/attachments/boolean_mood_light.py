# Mission: Boolean Mood Light
#
# Try changing the values below and see how bool(...) reacts.
# Your job is to:
#   1. Call bool(...) on each value.
#   2. Print the original value and the result of bool(...).
#   3. Notice which values are considered True or False in Python.

is_happy = True
name = ''       # empty string
coins = 0       # zero
message = 'hi'  # non-empty string

# TODO: for each variable above, print something like:
# is_happy = True -> bool(is_happy) = True
# name = '' -> bool(name) = False
# ... etc.

