# Transitions and Simple Animations

Transitions make changes smooth instead of instant.

```css
button {
  transition: background-color 0.25s ease;
}

button:hover {
  background-color: #1b5fc2;
}
```

Now the color change feels softer.
For bigger movement, CSS animations use `@keyframes`.
